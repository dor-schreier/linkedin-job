---
plan: 03-01
phase: 3
wave: 1
depends_on: []
files_modified:
  - app/repository.py
  - app/routes/jobs.py
  - app/templates/jobs.html
autonomous: true
requirements_addressed: [JOBS-01, JOBS-05, JOBS-06, JOBS-07, UI-02]

must_haves:
  truths:
    - "GET /jobs renders a page with all scraped jobs visible in the browser"
    - "Filter bar triggers HTMX partial update of #job-list without full page reload"
    - "Status dropdown on each card sends hx-post to /jobs/{id}/status and updates silently"
    - "Jobs with salary_min/salary_max display the actual salary range; jobs without show nothing"
    - "Empty state shows 'No jobs found' heading when no jobs exist"
  artifacts:
    - path: "app/repository.py"
      provides: "list_jobs extended with company and salary_min filter params"
      contains: "def list_jobs"
    - path: "app/routes/jobs.py"
      provides: "GET /jobs, POST /jobs/{id}/status routes"
      exports: ["router"]
    - path: "app/templates/jobs.html"
      provides: "Jobs list page with filter bar, job cards, status dropdowns, pagination"
      contains: "id=\"job-list\""
  key_links:
    - from: "app/templates/jobs.html filter form"
      to: "GET /jobs"
      via: "hx-get='/jobs' hx-trigger='change' hx-target='#job-list' hx-swap='innerHTML'"
    - from: "app/templates/jobs.html status select"
      to: "POST /jobs/{id}/status"
      via: "hx-post='/jobs/{id}/status' hx-swap='none'"
---

# Plan 03-01: Jobs List Page — Repository, Route, Template

## Objective

Build the Jobs List page: extend the repository with filter support, add FastAPI routes for listing and status-update, and implement the full Jinja2 + HTMX template per the UI-SPEC.

This is the primary user-facing feature of Phase 3: a paginated, filterable job list where users can browse scraped jobs and set per-job status.

## Threat Model

| Boundary | Description |
|----------|-------------|
| Browser → POST /jobs/{id}/status | Untrusted job_id and status value from form POST |
| Browser → GET /jobs | Untrusted filter query params (status, company, salary_min) |

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | POST /jobs/{id}/status | mitigate | Validate `status` against `JobStatus` enum before DB write; return 422 if invalid value. Validate `job_id` is a positive integer. |
| T-03-02 | Injection | GET /jobs company filter | mitigate | Pass company value as SQLAlchemy bound parameter (`.filter(Job.company.ilike(:v))`), never string-interpolated into SQL. |
| T-03-03 | Injection | GET /jobs salary_min filter | mitigate | Cast salary_min to `float` with try/except; reject non-numeric values with HTTP 422. |
| T-03-04 | Information Disclosure | Job description in template | accept | Personal-use app, no PII risk, all data is self-scraped. |

## Tasks

<task id="03-01-T01">
<title>Extend JobRepository.list_jobs with company and salary_min filters</title>
<read_first>
- app/repository.py — current list_jobs signature and filter pattern to extend
- app/models.py — Job model column names (company: String, salary_min: Float, status: SAEnum(JobStatus))
</read_first>
<action>
Modify `list_jobs` in `app/repository.py` to accept two new optional parameters:
- `company: Optional[str] = None` — case-insensitive substring filter using `Job.company.ilike(f"%{company}%")`
- `salary_min_filter: Optional[float] = None` — filters to jobs where `Job.salary_min >= salary_min_filter`

Updated signature:
```python
def list_jobs(
    self,
    status: Optional[JobStatus] = None,
    company: Optional[str] = None,
    salary_min_filter: Optional[float] = None,
    limit: int = 50,
    offset: int = 0,
) -> list[Job]:
    q = self.session.query(Job)
    if status:
        q = q.filter(Job.status == status)
    if company:
        q = q.filter(Job.company.ilike(f"%{company}%"))
    if salary_min_filter is not None:
        q = q.filter(Job.salary_min >= salary_min_filter)
    return q.order_by(Job.scraped_at.desc()).offset(offset).limit(limit).all()
```

Also add a `count_jobs_filtered` method to support pagination display:
```python
def count_jobs_filtered(
    self,
    status: Optional[JobStatus] = None,
    company: Optional[str] = None,
    salary_min_filter: Optional[float] = None,
) -> int:
    q = self.session.query(Job)
    if status:
        q = q.filter(Job.status == status)
    if company:
        q = q.filter(Job.company.ilike(f"%{company}%"))
    if salary_min_filter is not None:
        q = q.filter(Job.salary_min >= salary_min_filter)
    return q.count()
```

Keep all existing methods unchanged.
</action>
<acceptance_criteria>
- [ ] `grep -n "def list_jobs" app/repository.py` returns a line containing `company: Optional[str] = None`
- [ ] `grep -n "def list_jobs" app/repository.py` returns a line containing `salary_min_filter: Optional[float] = None`
- [ ] `grep -n "ilike" app/repository.py` returns at least one match (not string interpolation)
- [ ] `grep -n "def count_jobs_filtered" app/repository.py` returns a match
- [ ] `python -c "from app.repository import JobRepository; print('ok')"` exits 0
</acceptance_criteria>
</task>

<task id="03-01-T02">
<title>Create GET /jobs and POST /jobs/{id}/status routes</title>
<read_first>
- app/routes/scrape.py — existing route pattern: how session is obtained via Depends, how templates are rendered with TemplateResponse
- app/repository.py — updated list_jobs and count_jobs_filtered signatures
- app/models.py — JobStatus enum values: NEW, SAVED, APPLIED, INTERVIEWING, OFFER, REJECTED
- app/database.py — SessionLocal and get_db dependency pattern
</read_first>
<action>
Create `app/routes/jobs.py` with two routes:

```python
from typing import Optional
from fastapi import APIRouter, Depends, Request, Response, HTTPException
from fastapi.responses import HTMLResponse
from fastapi.templating import Jinja2Templates
from sqlalchemy.orm import Session

from app.database import get_db
from app.models import JobStatus
from app.repository import JobRepository

router = APIRouter()
templates = Jinja2Templates(directory="app/templates")

PAGE_SIZE = 50


@router.get("/jobs", response_class=HTMLResponse)
def jobs_list(
    request: Request,
    status: Optional[str] = None,
    company: Optional[str] = None,
    salary_min: Optional[str] = None,
    page: int = 1,
    db: Session = Depends(get_db),
):
    repo = JobRepository(db)

    # Validate and coerce status
    status_enum: Optional[JobStatus] = None
    if status:
        try:
            status_enum = JobStatus(status)
        except ValueError:
            raise HTTPException(status_code=422, detail=f"Invalid status: {status}")

    # Validate and coerce salary_min
    salary_min_f: Optional[float] = None
    if salary_min:
        try:
            salary_min_f = float(salary_min)
        except ValueError:
            raise HTTPException(status_code=422, detail="salary_min must be numeric")

    offset = (page - 1) * PAGE_SIZE
    jobs = repo.list_jobs(
        status=status_enum,
        company=company or None,
        salary_min_filter=salary_min_f,
        limit=PAGE_SIZE,
        offset=offset,
    )
    total = repo.count_jobs_filtered(
        status=status_enum,
        company=company or None,
        salary_min_filter=salary_min_f,
    )
    has_more = (offset + PAGE_SIZE) < total

    # Determine if this is an HTMX partial request
    is_htmx = request.headers.get("HX-Request") == "true"
    template = "partials/job_list.html" if is_htmx else "jobs.html"

    return templates.TemplateResponse(
        template,
        {
            "request": request,
            "jobs": jobs,
            "total": total,
            "page": page,
            "has_more": has_more,
            "filters": {
                "status": status or "",
                "company": company or "",
                "salary_min": salary_min or "",
            },
            "job_statuses": [s.value for s in JobStatus],
        },
    )


@router.post("/jobs/{job_id}/status", response_class=Response)
def update_job_status(
    job_id: int,
    request: Request,
    db: Session = Depends(get_db),
):
    import asyncio
    from fastapi import Form
    # Use sync form parsing via FormData — called via HTMX hx-post with form encoding
    raise HTTPException(status_code=405, detail="Use form POST")
```

Wait — FastAPI form parsing requires `Form`. Rewrite the status endpoint correctly:

```python
@router.post("/jobs/{job_id}/status")
async def update_job_status(
    job_id: int,
    status: str = Form(...),
    db: Session = Depends(get_db),
):
    if job_id <= 0:
        raise HTTPException(status_code=422, detail="Invalid job_id")
    try:
        status_enum = JobStatus(status)
    except ValueError:
        raise HTTPException(status_code=422, detail=f"Invalid status value: {status}")

    repo = JobRepository(db)
    job = repo.update_job_status(job_id, status_enum)
    if not job:
        raise HTTPException(status_code=404, detail="Job not found")
    # Return 204 No Content — HTMX hx-swap="none" expects no body
    return Response(status_code=204)
```

The final `app/routes/jobs.py` combines both. Import `Form` from fastapi at the top. The `jobs_list` route renders `jobs.html` for full page loads and `partials/job_list.html` for HTMX partial swaps (for the filter change trigger).

Create the directory `app/templates/partials/` for the partial template (created in T03).
</action>
<acceptance_criteria>
- [ ] `grep -n "router = APIRouter" app/routes/jobs.py` returns a match
- [ ] `grep -n "def jobs_list" app/routes/jobs.py` returns a match
- [ ] `grep -n "def update_job_status" app/routes/jobs.py` returns a match
- [ ] `grep -n "JobStatus(status)" app/routes/jobs.py` returns a match (enum validation present)
- [ ] `grep -n "status_code=204" app/routes/jobs.py` returns a match (silent POST response)
- [ ] `grep -n "HX-Request" app/routes/jobs.py` returns a match (HTMX partial detection)
- [ ] `python -c "from app.routes.jobs import router; print('ok')"` exits 0
</acceptance_criteria>
</task>

<task id="03-01-T03">
<title>Create jobs.html and partials/job_list.html templates</title>
<read_first>
- app/templates/scrape.html — existing template structure: CDN script tags, Tailwind classes, HTMX attributes, Jinja2 syntax pattern
- .planning/phases/03-web-ui-api/03-UI-SPEC.md — complete visual and interaction contract: color tokens, spacing, component specs, copywriting, HTMX patterns
</read_first>
<action>
Create `app/templates/jobs.html` as the full page (includes nav bar, filter bar, job list, pagination):

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Job Finder — Jobs</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://unpkg.com/htmx.org@2.0.4"></script>
  <style>
    .htmx-request { opacity: 0.5; transition: opacity 0.2s; }
  </style>
</head>
<body class="bg-gray-100 min-h-screen">

  <!-- Nav Bar -->
  <nav class="bg-white border-b border-gray-200 px-6 py-3 flex items-center justify-between">
    <span class="text-lg font-bold text-gray-800">Job Finder</span>
    <div class="flex gap-6">
      <a href="/jobs" class="text-sm text-blue-600 font-normal border-b-2 border-blue-600 pb-0.5">Jobs</a>
      <a href="/profile" class="text-sm text-gray-600 hover:text-gray-900">Profile</a>
      <a href="/search-config" class="text-sm text-gray-600 hover:text-gray-900">Search Config</a>
      <a href="/watch-rules" class="text-sm text-gray-600 hover:text-gray-900">Watch Rules</a>
    </div>
  </nav>

  <div class="max-w-4xl mx-auto px-4 py-8">
    <h1 class="text-2xl font-bold text-gray-800 mb-6">Jobs</h1>

    <!-- Filter Bar -->
    <form
      hx-get="/jobs"
      hx-trigger="change"
      hx-target="#job-list"
      hx-swap="innerHTML"
      hx-indicator="#job-list"
      class="bg-white border border-gray-200 rounded-lg p-4 mb-6 flex flex-wrap gap-4 items-end"
    >
      <div>
        <label for="filter-status" class="block text-sm font-normal text-gray-700 tracking-wide mb-1">Status</label>
        <select
          id="filter-status"
          name="status"
          class="border border-gray-300 rounded px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
        >
          <option value="" {% if not filters.status %}selected{% endif %}>All Statuses</option>
          {% for s in job_statuses %}
          <option value="{{ s }}" {% if filters.status == s %}selected{% endif %}>{{ s | capitalize }}</option>
          {% endfor %}
        </select>
      </div>
      <div>
        <label for="filter-company" class="block text-sm font-normal text-gray-700 tracking-wide mb-1">Company</label>
        <input
          id="filter-company"
          name="company"
          type="text"
          value="{{ filters.company }}"
          placeholder="Filter by company"
          class="border border-gray-300 rounded px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
        />
      </div>
      <div>
        <label for="filter-salary" class="block text-sm font-normal text-gray-700 tracking-wide mb-1">Min Salary</label>
        <input
          id="filter-salary"
          name="salary_min"
          type="number"
          value="{{ filters.salary_min }}"
          placeholder="e.g. 80000"
          class="border border-gray-300 rounded px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
        />
      </div>
      <a href="/jobs" class="text-sm text-gray-500 underline self-end pb-2">Reset</a>
    </form>

    <!-- Job List (partial target) -->
    <div id="job-list">
      {% include "partials/job_list.html" %}
    </div>
  </div>

</body>
</html>
```

Create `app/templates/partials/job_list.html` as the partial (rendered for both full load via include and HTMX partial swap):

```html
{% if jobs %}
  {% for job in jobs %}
  <div class="bg-white rounded-lg border border-gray-200 p-4 mb-3">
    <div class="flex items-start justify-between gap-4">
      <div class="flex-1 min-w-0">
        <p class="text-base font-bold text-gray-800 truncate">{{ job.title }}</p>
        <p class="text-sm text-gray-600 mt-0.5">{{ job.company }} · {{ job.location or "Location not listed" }}</p>
        <div class="flex flex-wrap items-center gap-2 mt-2">
          <span class="text-xs bg-gray-100 text-gray-600 px-2 py-0.5 rounded">{{ job.source }}</span>
          {% if job.salary_min and job.salary_max %}
          <span class="text-sm text-gray-700">
            {{ job.salary_currency or "$" }}{{ "{:,.0f}".format(job.salary_min) }} – {{ "{:,.0f}".format(job.salary_max) }}
          </span>
          {% elif job.salary_min %}
          <span class="text-sm text-gray-700">
            {{ job.salary_currency or "$" }}{{ "{:,.0f}".format(job.salary_min) }}+
          </span>
          {% endif %}
          <span class="text-xs text-gray-400">{{ job.scraped_at.strftime("%b %d, %Y") if job.scraped_at else "" }}</span>
        </div>
      </div>
      <div class="flex flex-col items-end gap-2 shrink-0">
        <select
          hx-post="/jobs/{{ job.id }}/status"
          hx-swap="none"
          name="status"
          aria-label="Job status for {{ job.title }}"
          class="border border-gray-300 rounded px-2 py-1 text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
        >
          <option value="" {% if not job.status or job.status.value == 'new' %}selected{% endif %}>— Set Status —</option>
          <option value="saved" {% if job.status and job.status.value == 'saved' %}selected{% endif %}>Saved</option>
          <option value="applied" {% if job.status and job.status.value == 'applied' %}selected{% endif %}>Applied</option>
          <option value="interviewing" {% if job.status and job.status.value == 'interviewing' %}selected{% endif %}>Interviewing</option>
          <option value="offer" {% if job.status and job.status.value == 'offer' %}selected{% endif %}>Offer</option>
          <option value="rejected" {% if job.status and job.status.value == 'rejected' %}selected{% endif %}>Rejected</option>
        </select>
        {% if job.apply_url %}
        <a href="{{ job.apply_url }}" target="_blank" rel="noopener noreferrer" class="text-sm text-blue-600 underline">View Job</a>
        {% endif %}
      </div>
    </div>
  </div>
  {% endfor %}

  {% if has_more %}
  <div class="text-center mt-4">
    <button
      hx-get="/jobs?page={{ page + 1 }}&status={{ filters.status }}&company={{ filters.company }}&salary_min={{ filters.salary_min }}"
      hx-swap="beforeend"
      hx-target="#job-list"
      class="text-sm text-blue-600 border border-blue-600 rounded px-4 py-2 hover:bg-blue-50"
    >
      Load more
    </button>
  </div>
  {% endif %}

{% else %}
  {% if filters.status or filters.company or filters.salary_min %}
  <div class="text-center py-12">
    <p class="text-base font-bold text-gray-800">No jobs found</p>
    <p class="text-sm text-gray-500 mt-1">No jobs match your filters. Try clearing one or more filters.</p>
  </div>
  {% else %}
  <div class="text-center py-12">
    <p class="text-base font-bold text-gray-800">No jobs found</p>
    <p class="text-sm text-gray-500 mt-1">Run a scrape to fetch jobs, or adjust your filters.</p>
  </div>
  {% endif %}
{% endif %}
```

Note: The `hx-post` on the status `<select>` sends the form field named `status` with the selected value. HTMX sends the changed element's value as a form-encoded POST body automatically on the `change` event (default trigger for `<select>`).
</action>
<acceptance_criteria>
- [ ] `grep -n "id=\"job-list\"" app/templates/jobs.html` returns a match
- [ ] `grep -n "hx-get=\"/jobs\"" app/templates/jobs.html` returns a match (filter form HTMX)
- [ ] `grep -n "hx-trigger=\"change\"" app/templates/jobs.html` returns a match
- [ ] `grep -n "border-b-2 border-blue-600" app/templates/jobs.html` returns a match (active nav indicator)
- [ ] File `app/templates/partials/job_list.html` exists
- [ ] `grep -n "hx-post=\"/jobs" app/templates/partials/job_list.html` returns a match (status dropdown)
- [ ] `grep -n "hx-swap=\"none\"" app/templates/partials/job_list.html` returns a match (silent status POST)
- [ ] `grep -n "aria-label" app/templates/partials/job_list.html` returns a match (accessibility)
- [ ] `grep -n "salary_min and job.salary_max" app/templates/partials/job_list.html` returns a match (JOBS-05 salary display)
- [ ] `grep -n "Load more" app/templates/partials/job_list.html` returns a match (pagination)
- [ ] `grep -n "No jobs found" app/templates/partials/job_list.html` returns a match (empty state)
- [ ] `grep -n "View Job" app/templates/partials/job_list.html` returns a match (apply link)
</acceptance_criteria>
</task>

## Verification

```bash
# Verify imports resolve
python -c "from app.routes.jobs import router; print('jobs router: ok')"

# Start server and verify /jobs returns 200
uvicorn app.main:app --reload &
sleep 2
curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/jobs
# Expected: 200

# Verify HTMX partial — filter request returns partial HTML not full page
curl -s -H "HX-Request: true" "http://localhost:8000/jobs?status=saved" | grep -c "job-list"
# Expected: 0 (partial template does not contain the wrapper div)

# Verify status POST rejects invalid value
curl -s -o /dev/null -w "%{http_code}" -X POST -d "status=invalid_value" http://localhost:8000/jobs/1/status
# Expected: 422
```

## must_haves

- GET /jobs returns HTTP 200 with HTML containing `id="job-list"`
- POST /jobs/{id}/status with a valid status value returns HTTP 204
- POST /jobs/{id}/status with an invalid status value returns HTTP 422
- Filter form has `hx-get="/jobs"`, `hx-trigger="change"`, `hx-target="#job-list"`, `hx-swap="innerHTML"`
- Status select has `hx-post`, `hx-swap="none"`, and `aria-label`
- Salary is rendered only when `salary_min` is not null (never renders empty salary block)
- Empty state shows "No jobs found" when jobs list is empty
