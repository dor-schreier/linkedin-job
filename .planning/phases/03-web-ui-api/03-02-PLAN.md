---
plan: 03-02
phase: 3
wave: 2
depends_on: [03-01]
files_modified:
  - app/main.py
  - app/routes/pages.py
  - app/templates/profile.html
  - app/templates/watch_rules.html
  - app/templates/search_config.html
autonomous: true
requirements_addressed: [UI-01, UI-03, UI-04]

must_haves:
  truths:
    - "All four nav links (Jobs, Profile, Search Config, Watch Rules) resolve with HTTP 200"
    - "Navigating to /search-config renders the existing scrape form (not a 404)"
    - "Profile and Watch Rules pages render a placeholder with the nav bar and correct page title"
    - "The app starts without import errors after all routers are registered"
  artifacts:
    - path: "app/main.py"
      provides: "All four routers registered: health, scrape, jobs, pages"
      contains: "from app.routes.pages import router as pages_router"
    - path: "app/routes/pages.py"
      provides: "GET /profile, GET /watch-rules, GET /search-config routes"
      exports: ["router"]
    - path: "app/templates/profile.html"
      provides: "Placeholder profile page with nav bar"
      contains: "Job Finder — Profile"
    - path: "app/templates/watch_rules.html"
      provides: "Placeholder watch rules page with nav bar"
      contains: "Job Finder — Watch Rules"
    - path: "app/templates/search_config.html"
      provides: "Search Config page (rename/extend of scrape.html content) with nav bar"
      contains: "Job Finder — Search Config"
  key_links:
    - from: "app/main.py"
      to: "app/routes/pages.py"
      via: "app.include_router(pages_router)"
    - from: "app/main.py"
      to: "app/routes/jobs.py"
      via: "app.include_router(jobs_router)"
---

# Plan 03-02: Navigation Wiring — Pages Router, Placeholder Templates, main.py

## Objective

Wire all navigation routes so that every nav link resolves without a 404. This plan:
1. Creates `routes/pages.py` with GET handlers for `/profile`, `/watch-rules`, and `/search-config`
2. Creates placeholder templates for Profile and Watch Rules pages (full nav bar, correct page title, "coming soon" body)
3. Creates `search_config.html` as the canonical Search Config page with nav bar (contains the existing scrape form)
4. Registers all routers in `main.py`

After this plan, the app satisfies UI-03 (all nav links resolve) and UI-04 (working app state in browser).

## Threat Model

| Boundary | Description |
|----------|-------------|
| Browser → GET /profile, /watch-rules, /search-config | Read-only GET routes, no user input |

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Information Disclosure | Placeholder pages | accept | Pages contain no sensitive data; personal-use app with no auth. |
| T-03-06 | Denial of Service | All routes in main.py | accept | Single-user local app; no rate limiting needed. |

## Tasks

<task id="03-02-T01">
<title>Create routes/pages.py with /profile, /watch-rules, /search-config routes</title>
<read_first>
- app/routes/scrape.py — existing route pattern: Jinja2Templates instantiation, TemplateResponse call, get_db Depends pattern
- app/routes/health.py — minimal router pattern for reference
- app/templates/scrape.html — content to be served at /search-config (rename to search_config.html)
</read_first>
<action>
Create `app/routes/pages.py`:

```python
from fastapi import APIRouter, Depends, Request
from fastapi.responses import HTMLResponse
from fastapi.templating import Jinja2Templates
from sqlalchemy.orm import Session

from app.database import get_db
from app.repository import JobRepository

router = APIRouter()
templates = Jinja2Templates(directory="app/templates")


@router.get("/search-config", response_class=HTMLResponse)
def search_config(request: Request, db: Session = Depends(get_db)):
    repo = JobRepository(db)
    configs = repo.list_search_configs(active_only=True)
    latest_config = configs[-1] if configs else None
    # Import scrape status from the scrape route module
    from app.routes.scrape import _scrape_status
    return templates.TemplateResponse(
        "search_config.html",
        {"request": request, "latest_config": latest_config, "status": _scrape_status},
    )


@router.get("/profile", response_class=HTMLResponse)
def profile_page(request: Request):
    return templates.TemplateResponse("profile.html", {"request": request})


@router.get("/watch-rules", response_class=HTMLResponse)
def watch_rules_page(request: Request):
    return templates.TemplateResponse("watch_rules.html", {"request": request})
```

Note: `/scrape` route in `scrape.py` continues to handle POST /scrape/run and GET /scrape/status as before. The `/search-config` GET route here is the navigation destination for the Search Config nav link. Check `app/routes/scrape.py` for the exact name of the `_scrape_status` global — if it differs, use the correct import.

If `_scrape_status` is a module-level dict/object in scrape.py (check the file), import it with `from app.routes.scrape import _scrape_status`. If it is named differently (e.g., `scrape_status`, `_status`), use that name. Do NOT assume the name — read scrape.py first.
</action>
<acceptance_criteria>
- [ ] `grep -n "router = APIRouter" app/routes/pages.py` returns a match
- [ ] `grep -n "def search_config" app/routes/pages.py` returns a match
- [ ] `grep -n "def profile_page" app/routes/pages.py` returns a match
- [ ] `grep -n "def watch_rules_page" app/routes/pages.py` returns a match
- [ ] `grep -n "/search-config" app/routes/pages.py` returns a match
- [ ] `python -c "from app.routes.pages import router; print('ok')"` exits 0
</acceptance_criteria>
</task>

<task id="03-02-T02">
<title>Create search_config.html, profile.html, watch_rules.html templates and update main.py</title>
<read_first>
- app/templates/scrape.html — full content to migrate into search_config.html (add nav bar, update title)
- app/templates/jobs.html — nav bar HTML to copy exactly (active link changes per page)
- app/main.py — current router registrations to extend
- .planning/phases/03-web-ui-api/03-UI-SPEC.md — nav bar spec: active link class `text-blue-600 font-normal border-b-2 border-blue-600`, inactive `text-gray-600 hover:text-gray-900`
</read_first>
<action>
**1. Create `app/templates/search_config.html`**

Copy the full content of `scrape.html` and:
- Change `<title>` to `Job Finder — Search Config`
- Add the nav bar from jobs.html immediately after `<body>`, with "Search Config" as the active link (receives `text-blue-600 font-normal border-b-2 border-blue-600`) and all others inactive
- Wrap the existing form card in `<div class="max-w-xl mx-auto px-4 py-8">` to match page layout
- Keep all existing form fields, HTMX attributes, and scrape result div exactly as in scrape.html

Nav bar for search_config.html (active = Search Config):
```html
<nav class="bg-white border-b border-gray-200 px-6 py-3 flex items-center justify-between">
  <span class="text-lg font-bold text-gray-800">Job Finder</span>
  <div class="flex gap-6">
    <a href="/jobs" class="text-sm text-gray-600 hover:text-gray-900">Jobs</a>
    <a href="/profile" class="text-sm text-gray-600 hover:text-gray-900">Profile</a>
    <a href="/search-config" class="text-sm text-blue-600 font-normal border-b-2 border-blue-600 pb-0.5">Search Config</a>
    <a href="/watch-rules" class="text-sm text-gray-600 hover:text-gray-900">Watch Rules</a>
  </div>
</nav>
```

**2. Create `app/templates/profile.html`**

Placeholder page with nav bar (active = Profile):
```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Job Finder — Profile</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://unpkg.com/htmx.org@2.0.4"></script>
</head>
<body class="bg-gray-100 min-h-screen">
  <nav class="bg-white border-b border-gray-200 px-6 py-3 flex items-center justify-between">
    <span class="text-lg font-bold text-gray-800">Job Finder</span>
    <div class="flex gap-6">
      <a href="/jobs" class="text-sm text-gray-600 hover:text-gray-900">Jobs</a>
      <a href="/profile" class="text-sm text-blue-600 font-normal border-b-2 border-blue-600 pb-0.5">Profile</a>
      <a href="/search-config" class="text-sm text-gray-600 hover:text-gray-900">Search Config</a>
      <a href="/watch-rules" class="text-sm text-gray-600 hover:text-gray-900">Watch Rules</a>
    </div>
  </nav>
  <div class="max-w-4xl mx-auto px-4 py-8">
    <h1 class="text-2xl font-bold text-gray-800 mb-4">Profile</h1>
    <p class="text-sm text-gray-500">Profile editing coming in Phase 4.</p>
  </div>
</body>
</html>
```

**3. Create `app/templates/watch_rules.html`**

Placeholder page with nav bar (active = Watch Rules):
```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Job Finder — Watch Rules</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://unpkg.com/htmx.org@2.0.4"></script>
</head>
<body class="bg-gray-100 min-h-screen">
  <nav class="bg-white border-b border-gray-200 px-6 py-3 flex items-center justify-between">
    <span class="text-lg font-bold text-gray-800">Job Finder</span>
    <div class="flex gap-6">
      <a href="/jobs" class="text-sm text-gray-600 hover:text-gray-900">Jobs</a>
      <a href="/profile" class="text-sm text-gray-600 hover:text-gray-900">Profile</a>
      <a href="/search-config" class="text-sm text-gray-600 hover:text-gray-900">Search Config</a>
      <a href="/watch-rules" class="text-sm text-blue-600 font-normal border-b-2 border-blue-600 pb-0.5">Watch Rules</a>
    </div>
  </nav>
  <div class="max-w-4xl mx-auto px-4 py-8">
    <h1 class="text-2xl font-bold text-gray-800 mb-4">Watch Rules</h1>
    <p class="text-sm text-gray-500">Watch rule management coming in Phase 5.</p>
  </div>
</body>
</html>
```

**4. Update `app/main.py`**

Add imports for the two new routers and register them:

```python
from contextlib import asynccontextmanager
from fastapi import FastAPI
from app.database import init_db
from app.routes.health import router as health_router
from app.routes.scrape import router as scrape_router
from app.routes.jobs import router as jobs_router
from app.routes.pages import router as pages_router


@asynccontextmanager
async def lifespan(app: FastAPI):
    init_db()
    yield


app = FastAPI(title="Job Finder", lifespan=lifespan)
app.include_router(health_router)
app.include_router(scrape_router)
app.include_router(jobs_router)
app.include_router(pages_router)
```

Do not change the lifespan or existing router registrations. Only add the two new imports and two new `include_router` calls.
</action>
<acceptance_criteria>
- [ ] `grep -n "Job Finder — Search Config" app/templates/search_config.html` returns a match
- [ ] `grep -n "border-b-2 border-blue-600" app/templates/search_config.html` returns a match (active nav on Search Config link)
- [ ] `grep -n "hx-post=\"/scrape/run\"" app/templates/search_config.html` returns a match (form preserved)
- [ ] `grep -n "Job Finder — Profile" app/templates/profile.html` returns a match
- [ ] `grep -n "border-b-2 border-blue-600" app/templates/profile.html` returns a match (active nav on Profile link)
- [ ] `grep -n "Job Finder — Watch Rules" app/templates/watch_rules.html` returns a match
- [ ] `grep -n "border-b-2 border-blue-600" app/templates/watch_rules.html` returns a match (active nav on Watch Rules link)
- [ ] `grep -n "from app.routes.jobs import router as jobs_router" app/main.py` returns a match
- [ ] `grep -n "from app.routes.pages import router as pages_router" app/main.py` returns a match
- [ ] `grep -n "app.include_router(jobs_router)" app/main.py` returns a match
- [ ] `grep -n "app.include_router(pages_router)" app/main.py` returns a match
- [ ] `python -c "from app.main import app; print('ok')"` exits 0
</acceptance_criteria>
</task>

## Verification

```bash
# Verify app starts without errors
python -c "from app.main import app; print('app import: ok')"

# Start server
uvicorn app.main:app --reload &
sleep 2

# Verify all nav routes resolve
curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/jobs
# Expected: 200

curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/profile
# Expected: 200

curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/search-config
# Expected: 200

curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/watch-rules
# Expected: 200

# Verify page titles
curl -s http://localhost:8000/profile | grep -c "Job Finder — Profile"
# Expected: 1

curl -s http://localhost:8000/watch-rules | grep -c "Job Finder — Watch Rules"
# Expected: 1

curl -s http://localhost:8000/search-config | grep -c "Job Finder — Search Config"
# Expected: 1
```

## must_haves

- `python -c "from app.main import app"` exits 0 with no ImportError
- GET /jobs, /profile, /search-config, /watch-rules all return HTTP 200
- Each page has a `<title>` matching "Job Finder — {Page Name}"
- Each nav link has `border-b-2 border-blue-600` only on the current page's link
- search_config.html contains the scrape form (`hx-post="/scrape/run"`) unchanged
- app/main.py registers exactly 4 routers: health, scrape, jobs, pages
