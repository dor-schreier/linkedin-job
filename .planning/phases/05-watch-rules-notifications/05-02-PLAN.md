---
phase: 5
plan: 02
type: feature
wave: 2
depends_on: [05-01-PLAN.md]
files_modified:
  - app/templates/partials/nav.html
  - app/templates/watch_rules.html
  - app/templates/watch_matches.html
  - app/templates/jobs.html
  - app/templates/profile.html
  - app/templates/search_config.html
  - app/templates/scrape.html
  - app/templates/health.html
  - app/routes/pages.py
  - app/routes/jobs.py
  - app/routes/scrape.py
  - app/routes/health.py
  - app/routes/watch.py
autonomous: true
requirements: [WTCH-05, WTCH-06, WTCH-07]
must_haves:
  truths:
    - "GET /watch-rules renders a form to create rules and a list of existing rules with a delete button"
    - "GET /watch-matches renders all notifications joined with jobs and marks them read on view"
    - "Every page's nav shows a 'Matches' link with a red badge count when unread notifications exist"
    - "Clicking 'Matches' navigates to /watch-matches and the badge count becomes 0 on next page load"
  artifacts:
    - path: "app/templates/partials/nav.html"
      provides: "Shared nav include with Matches link + badge rendered from {{ unread_count }}"
    - path: "app/templates/watch_rules.html"
      provides: "Full CRUD UI wired to /watch-rules/create and /watch-rules/{id}/delete"
    - path: "app/templates/watch_matches.html"
      provides: "Watched-match list view"
  key_links:
    - from: "app/templates/partials/nav.html"
      to: "/watch-matches"
      via: "anchor tag + badge span bound to unread_count"
      pattern: "watch-matches"
    - from: "app/routes/pages.py get /watch-matches"
      to: "repository.mark_notifications_read()"
      via: "called after fetching notifications so badge resets"
      pattern: "mark_notifications_read"
    - from: "all page routes"
      to: "templates with unread_count in context"
      via: "helper _ctx(db, active) that injects count into every full-page TemplateResponse"
      pattern: "unread_count"
---

<objective>
Deliver the watch-rules and watch-matches UI: replace the placeholder watch_rules.html with a working form + list, create a new watch_matches.html page, add a shared nav partial with an unread-count badge, and update every existing page template to use it. Wire two new GET routes in pages.py and update every full-page route handler across pages.py, jobs.py, scrape.py, and health.py to inject `active` + `unread_count` via a shared `_ctx()` helper.

Purpose: Satisfy WTCH-05 (nav badge), WTCH-06 (dedicated matches section), and complete WTCH-07 (UI-level CRUD).
Output: One new partial, one new page template, one rewritten template, five updated templates, two new routes, a small context helper used by every full-page handler.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@app/routes/pages.py
@app/routes/jobs.py
@app/routes/scrape.py
@app/routes/health.py
@app/routes/watch.py
@app/templates/jobs.html
@app/templates/watch_rules.html
@app/templates/profile.html
@app/templates/search_config.html
@app/templates/scrape.html
@app/templates/health.html

<interfaces>
From Plan 05-01 (must already exist):
- POST /watch-rules/create  form fields: rule_type, value
- POST /watch-rules/{rule_id}/delete
- POST /notifications/mark-read
- GET  /api/notifications/unread-count -> {"count": int}
- repository.list_unread_notifications_with_jobs() -> list[(Notification, Job, WatchRule)]
- repository.list_all_notifications_with_jobs(limit=200) -> list[(Notification, Job, WatchRule)]
- repository.mark_notifications_read() -> int
- repository.count_unread_notifications() -> int
- repository.list_watch_rules(active_only=True) -> list[WatchRule]

Route file layout (IMPORTANT — handlers are spread across four files):
- app/routes/pages.py: search_config, profile_page, watch_rules_page (+ new watch_matches_page)
- app/routes/jobs.py:  jobs_page (GET /jobs) — full-page TemplateResponse at ~line 80
- app/routes/scrape.py: scrape_page (GET /scrape) — full-page TemplateResponse at ~line 47
- app/routes/health.py: health_page (GET /) — full-page TemplateResponse at ~line 23

Note: jobs.py also renders `partials/job_list.html` (HTMX partial) and `partials/job_score.html` (HTMX partial). These are fragment responses, NOT full pages — they do NOT include the nav partial and therefore do NOT need `active`/`unread_count` context. Only touch the full-page branches.

Existing nav markup (replicated in every template) looks like:
```html
<nav class="bg-white border-b border-gray-200 px-6 py-3 flex items-center justify-between">
  <span class="text-lg font-bold text-gray-800">Job Finder</span>
  <div class="flex gap-6">
    <a href="/jobs" ...>Jobs</a>
    <a href="/profile" ...>Profile</a>
    <a href="/search-config" ...>Search Config</a>
    <a href="/watch-rules" ...>Watch Rules</a>
  </div>
</nav>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared nav partial with badge + add GET routes /watch-rules and /watch-matches + wire _ctx() into every full-page handler across pages.py, jobs.py, scrape.py, health.py</name>
  <files>app/templates/partials/nav.html, app/routes/pages.py, app/routes/jobs.py, app/routes/scrape.py, app/routes/health.py</files>
  <read_first>
    - app/templates/jobs.html (current nav markup to replicate)
    - app/templates/watch_rules.html (current placeholder)
    - app/routes/pages.py (existing watch_rules_page stub to replace; Jinja2Templates pattern; search_config + profile_page handlers)
    - app/routes/jobs.py (jobs_page handler; note partials branch must NOT get _ctx)
    - app/routes/scrape.py (scrape_page handler)
    - app/routes/health.py (health_page handler at GET /)
    - app/repository.py (list_watch_rules, list_unread_notifications_with_jobs, list_all_notifications_with_jobs, count_unread_notifications, mark_notifications_read)
  </read_first>
  <action>
    STEP 1 — Create `app/templates/partials/nav.html`:

    ```html
    {# Shared nav. Expects: active (str), unread_count (int) in template context. #}
    <nav class="bg-white border-b border-gray-200 px-6 py-3 flex items-center justify-between">
      <span class="text-lg font-bold text-gray-800">Job Finder</span>
      <div class="flex gap-6 items-center">
        {% set items = [
          ('jobs', '/jobs', 'Jobs'),
          ('profile', '/profile', 'Profile'),
          ('search-config', '/search-config', 'Search Config'),
          ('watch-rules', '/watch-rules', 'Watch Rules'),
        ] %}
        {% for key, href, label in items %}
          <a href="{{ href }}" class="text-sm {% if active == key %}text-blue-600 font-normal border-b-2 border-blue-600 pb-0.5{% else %}text-gray-600 hover:text-gray-900{% endif %}">{{ label }}</a>
        {% endfor %}
        <a href="/watch-matches" class="relative text-sm {% if active == 'watch-matches' %}text-blue-600 font-normal border-b-2 border-blue-600 pb-0.5{% else %}text-gray-600 hover:text-gray-900{% endif %}">
          Matches
          {% if unread_count and unread_count > 0 %}
            <span class="ml-1 inline-flex items-center justify-center text-xs font-bold text-white bg-red-600 rounded-full px-1.5 py-0.5 min-w-[1.25rem]">{{ unread_count }}</span>
          {% endif %}
        </a>
      </div>
    </nav>
    ```

    STEP 2 — Create the shared `_ctx()` helper and apply it to EVERY full-page route handler across ALL four route files. The helper lives in `app/routes/pages.py`; the other three files import it.

    a) In `app/routes/pages.py`, add a module-level helper directly below `templates = Jinja2Templates(...)`:
    ```python
    def _ctx(db: Session, active: str, extra: Optional[dict] = None) -> dict:
        repo = JobRepository(db)
        base = {"active": active, "unread_count": repo.count_unread_notifications()}
        if extra:
            base.update(extra)
        return base
    ```
    Ensure `Optional` is imported from `typing` and `JobRepository` / `Session` / `get_session` imports are present (they already are — verify, do not duplicate).

    b) Replace the existing `watch_rules_page` function with:
    ```python
    @router.get("/watch-rules", response_class=HTMLResponse)
    def watch_rules_page(request: Request, db: Session = Depends(get_session)):
        repo = JobRepository(db)
        rules = repo.list_watch_rules(active_only=True)
        return templates.TemplateResponse(
            request,
            "watch_rules.html",
            _ctx(db, "watch-rules", {"rules": rules}),
        )
    ```

    c) Add a new route below it:
    ```python
    @router.get("/watch-matches", response_class=HTMLResponse)
    def watch_matches_page(request: Request, db: Session = Depends(get_session)):
        repo = JobRepository(db)
        # Fetch BEFORE marking read so the page shows what was just new.
        rows = repo.list_all_notifications_with_jobs(limit=200)
        repo.mark_notifications_read()
        # unread_count is recomputed inside _ctx AFTER the mark, so badge will be 0.
        return templates.TemplateResponse(
            request,
            "watch_matches.html",
            _ctx(db, "watch-matches", {"rows": rows}),
        )
    ```

    d) Apply `_ctx(db, "<active-key>", {...existing context...})` to EVERY full-page route handler across ALL four route files. The rule: any handler that renders a full-page template (the ones that include `partials/nav.html`) MUST go through `_ctx()`. HTMX partial branches (`partials/job_list.html`, `partials/job_score.html`, `partials/scrape_status.html`, etc.) MUST NOT be changed.

    The complete list of handlers to update, with exact active keys:

    ---
    **d1) `app/routes/pages.py` — `search_config` (active key: `"search-config"`)**
    ```python
    return templates.TemplateResponse(
        request,
        "search_config.html",
        _ctx(db, "search-config", {"latest_config": latest_config, "status": _scrape_status}),
    )
    ```

    **d2) `app/routes/pages.py` — `profile_page` (active key: `"profile"`)**
    ```python
    return templates.TemplateResponse(
        request,
        "profile.html",
        _ctx(db, "profile", {"profile": profile, "bullets": bullets}),
    )
    ```

    **d3) `app/routes/jobs.py` — `jobs_page` (active key: `"jobs"`)**
    Add at top of file: `from app.routes.pages import _ctx`
    Ensure the handler receives a db session (`db: Session = Depends(get_session)`) if it does not already — it does. Update ONLY the full-page branch (the one rendering `"jobs.html"`, NOT the `partials/job_list.html` HTMX branch). Keep the existing context dict intact — wrap it in `_ctx()`:
    ```python
    # full-page branch only (is_htmx == False)
    return templates.TemplateResponse(
        request,
        template,  # "jobs.html"
        _ctx(db, "jobs", {
            "jobs": jobs,
            "total": total,
            "page": page,
            "has_more": has_more,
            "filters": {
                "status": status or "",
                "company": company or "",
                "salary_min": salary_min or "",
            },
            "job_statuses": [s.value for s in JobStatus],
        }),
    )
    ```
    If the current implementation uses a single `TemplateResponse` call for both HTMX-partial and full-page branches, split it into an `if is_htmx:` branch (unchanged, no `_ctx`) and an `else:` branch (wrapped in `_ctx`). Do NOT touch the `/jobs/{job_id}/score` route — it returns `partials/job_score.html`, an HTMX fragment.

    **d4) `app/routes/scrape.py` — `scrape_page` (active key: `"scrape"`)**
    Add at top of file: `from app.routes.pages import _ctx`
    Update the handler to accept `db: Session = Depends(get_session)` if it does not already (it uses `session` — rename to `db` or add a separate `db` parameter; pick whichever is cleaner with the existing code). Then:
    ```python
    return templates.TemplateResponse(
        request,
        "scrape.html",
        _ctx(db, "scrape", {<existing context keys and values>}),
    )
    ```
    Do NOT touch `GET /scrape/status` — it returns an HTMX status fragment.

    **d5) `app/routes/health.py` — `health_page` (active key: `"health"`)**
    Add at top of file: `from app.routes.pages import _ctx`
    Add `db: Session = Depends(get_session)` to the handler signature (along with the necessary imports: `from sqlalchemy.orm import Session`, `from app.database import get_session`). Then:
    ```python
    return templates.TemplateResponse(
        request,
        "health.html",
        _ctx(db, "health", {<existing context keys and values>}),
    )
    ```

    ---
    **Rule of thumb (make this explicit to yourself before finishing Task 1):** Apply `_ctx(db, "<active-key>")` to EVERY handler that renders a full-page template using the shared nav partial. If a grep for `TemplateResponse` across `app/routes/` shows any full-page render (non-`partials/*.html`) that does NOT go through `_ctx`, Task 1 is incomplete.

    Note: the `"watch-rules"` and `"watch-matches"` keys are NOT in the nav partial's `items` loop on purpose — `watch-rules` is handled by the loop (key `'watch-rules'` matches), and `watch-matches` is handled by the standalone Matches anchor. Keys `"health"` and `"scrape"` will simply not light up any nav item (no active highlight), which is fine — those pages just render the nav without a highlighted link.
  </action>
  <verify>
    <automated>python -c "from app.main import app; from starlette.testclient import TestClient; c=TestClient(app)
for path in ['/watch-rules','/watch-matches','/jobs','/profile','/search-config','/scrape','/']:
    r=c.get(path); assert r.status_code in (200,307), (path, r.status_code)
    if r.status_code == 200:
        assert 'Matches' in r.text, f'{path} missing Matches link'
print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - File app/templates/partials/nav.html exists
    - grep -n "/watch-matches" app/templates/partials/nav.html returns a match
    - grep -n "unread_count" app/templates/partials/nav.html returns at least one match
    - grep -n "def _ctx" app/routes/pages.py returns a match
    - grep -n "watch_matches_page" app/routes/pages.py returns a match
    - grep -n "list_all_notifications_with_jobs" app/routes/pages.py returns a match
    - grep -n "mark_notifications_read" app/routes/pages.py returns a match
    - grep -n "from app.routes.pages import _ctx" app/routes/jobs.py returns a match
    - grep -n "from app.routes.pages import _ctx" app/routes/scrape.py returns a match
    - grep -n "from app.routes.pages import _ctx" app/routes/health.py returns a match
    - grep -n '_ctx(db, "jobs"' app/routes/jobs.py returns a match
    - grep -n '_ctx(db, "scrape"' app/routes/scrape.py returns a match
    - grep -n '_ctx(db, "health"' app/routes/health.py returns a match
    - grep -n '_ctx(db, "search-config"' app/routes/pages.py returns a match
    - grep -n '_ctx(db, "profile"' app/routes/pages.py returns a match
    - The verify command prints "ok"
  </acceptance_criteria>
  <done>Shared nav partial exists, pages.py exposes GET /watch-rules and GET /watch-matches, and EVERY full-page route handler in pages.py, jobs.py, scrape.py, and health.py goes through _ctx() to inject active + unread_count. HTMX partial branches remain untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite watch_rules.html, create watch_matches.html, and swap nav in all existing templates</name>
  <files>app/templates/watch_rules.html, app/templates/watch_matches.html, app/templates/jobs.html, app/templates/profile.html, app/templates/search_config.html, app/templates/scrape.html, app/templates/health.html</files>
  <read_first>
    - app/templates/watch_rules.html (current placeholder — to be fully replaced)
    - app/templates/jobs.html (nav block to remove + include pattern)
    - app/templates/profile.html
    - app/templates/search_config.html
    - app/templates/scrape.html
    - app/templates/health.html
    - app/templates/partials/nav.html (from Task 1 — include target)
  </read_first>
  <action>
    STEP 1 — Replace ENTIRE contents of `app/templates/watch_rules.html` with:

    ```html
    <!DOCTYPE html>
    <html lang="en">
    <head>
      <meta charset="UTF-8" />
      <meta name="viewport" content="width=device-width, initial-scale=1.0" />
      <title>Job Finder — Watch Rules</title>
      <script src="https://cdn.tailwindcss.com"></script>
      <script src="https://unpkg.com/htmx.org@2.0.4"></script>
    </head>
    <body class="bg-gray-100 min-h-screen">
      {% include "partials/nav.html" %}
      <div class="max-w-4xl mx-auto px-4 py-8">
        <h1 class="text-2xl font-bold text-gray-800 mb-4">Watch Rules</h1>
        <p class="text-sm text-gray-500 mb-6">Rules run after every scrape. Matching jobs appear in <a href="/watch-matches" class="text-blue-600 underline">Matches</a>.</p>

        <form action="/watch-rules/create" method="post" class="bg-white border border-gray-200 rounded-lg p-4 mb-6 flex flex-wrap gap-3 items-end">
          <div>
            <label class="block text-xs text-gray-700 mb-1">Rule type</label>
            <select name="rule_type" class="border border-gray-300 rounded px-2 py-1 text-sm">
              <option value="company">Company (exact)</option>
              <option value="keyword">Keyword in title</option>
              <option value="sector">Sector (description contains)</option>
            </select>
          </div>
          <div class="flex-1 min-w-[200px]">
            <label class="block text-xs text-gray-700 mb-1">Value</label>
            <input type="text" name="value" required minlength="1" maxlength="300" class="w-full border border-gray-300 rounded px-2 py-1 text-sm" />
          </div>
          <button type="submit" class="bg-blue-600 text-white text-sm px-4 py-1.5 rounded hover:bg-blue-700">Add rule</button>
        </form>

        <div class="bg-white border border-gray-200 rounded-lg">
          {% if rules %}
            <ul class="divide-y divide-gray-200">
              {% for rule in rules %}
                <li class="p-3 flex items-center justify-between">
                  <div>
                    <span class="text-xs uppercase text-gray-500 mr-2">{{ rule.rule_type }}</span>
                    <span class="text-sm text-gray-800">{{ rule.value }}</span>
                  </div>
                  <form action="/watch-rules/{{ rule.id }}/delete" method="post">
                    <button type="submit" class="text-red-600 text-xs hover:underline">Delete</button>
                  </form>
                </li>
              {% endfor %}
            </ul>
          {% else %}
            <p class="p-4 text-sm text-gray-500">No rules yet. Add one above.</p>
          {% endif %}
        </div>
      </div>
    </body>
    </html>
    ```

    STEP 2 — Create `app/templates/watch_matches.html`:

    ```html
    <!DOCTYPE html>
    <html lang="en">
    <head>
      <meta charset="UTF-8" />
      <meta name="viewport" content="width=device-width, initial-scale=1.0" />
      <title>Job Finder — Matches</title>
      <script src="https://cdn.tailwindcss.com"></script>
    </head>
    <body class="bg-gray-100 min-h-screen">
      {% include "partials/nav.html" %}
      <div class="max-w-4xl mx-auto px-4 py-8">
        <h1 class="text-2xl font-bold text-gray-800 mb-4">Watch Matches</h1>
        {% if rows %}
          <ul class="space-y-3">
            {% for notif, job, rule in rows %}
              <li class="bg-white border border-gray-200 rounded-lg p-4">
                <div class="flex justify-between items-start">
                  <div>
                    <a href="{{ job.apply_url or '#' }}" target="_blank" class="text-base font-semibold text-blue-600 hover:underline">{{ job.title }}</a>
                    <p class="text-sm text-gray-700">{{ job.company }}{% if job.location %} — {{ job.location }}{% endif %}</p>
                  </div>
                  <div class="text-right">
                    <span class="text-xs text-gray-500">matched rule</span>
                    <div class="text-xs text-gray-800">{{ rule.rule_type }}: {{ rule.value }}</div>
                  </div>
                </div>
              </li>
            {% endfor %}
          </ul>
        {% else %}
          <p class="text-sm text-gray-500">No watch matches yet. Add rules on the <a href="/watch-rules" class="text-blue-600 underline">Watch Rules</a> page, then run a scrape.</p>
        {% endif %}
      </div>
    </body>
    </html>
    ```

    STEP 3 — In each of `jobs.html`, `profile.html`, `search_config.html`, `scrape.html`, `health.html`:

    DELETE the entire existing `<nav ...>...</nav>` block (the block that contains links to /jobs, /profile, /search-config, /watch-rules).

    REPLACE it with exactly:
    ```html
    {% include "partials/nav.html" %}
    ```

    Do not touch anything else in those files.
  </action>
  <verify>
    <automated>python -c "from app.main import app; from starlette.testclient import TestClient; c=TestClient(app)
for path in ['/jobs','/profile','/search-config','/watch-rules','/watch-matches','/scrape','/']:
    r=c.get(path); assert r.status_code in (200,307), (path,r.status_code)
    if r.status_code == 200:
        assert 'Matches' in r.text, path
print('all pages ok')"</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "partials/nav.html" app/templates/jobs.html returns a match
    - grep -n "partials/nav.html" app/templates/profile.html returns a match
    - grep -n "partials/nav.html" app/templates/search_config.html returns a match
    - grep -n "partials/nav.html" app/templates/scrape.html returns a match
    - grep -n "partials/nav.html" app/templates/health.html returns a match
    - grep -n "partials/nav.html" app/templates/watch_rules.html returns a match
    - grep -n "partials/nav.html" app/templates/watch_matches.html returns a match
    - grep -c "Watch Rule management coming in Phase 5" app/templates/watch_rules.html returns 0
    - grep -n "/watch-rules/create" app/templates/watch_rules.html returns a match
    - grep -n "/watch-rules/{{ rule.id }}/delete" app/templates/watch_rules.html returns a match
    - File app/templates/watch_matches.html exists and contains "Watch Matches"
    - grep -n 'href="/watch-rules"' app/templates/jobs.html returns 0 matches (nav moved to partial)
    - Verify command prints "all pages ok"
  </acceptance_criteria>
  <done>watch_rules.html is a working CRUD page; watch_matches.html shows matches; all other templates include the shared nav partial with the badge.</done>
</task>

</tasks>

<verification>
- Start server, visit /watch-rules — create a rule, it appears in list; delete, it disappears
- Run a scrape that matches the rule — /watch-matches shows the job, nav badge shows count on other pages before visiting
- Visit /watch-matches — badge resets to 0 on next page load of any page
- All existing pages (/jobs, /profile, /search-config, /scrape, /) render without 500s and show the new Matches link
</verification>

<success_criteria>
WTCH-05 (nav badge for unread count), WTCH-06 (dedicated matches section), WTCH-07 (add/delete rules from UI) satisfied. Badge correctly resets after visiting /watch-matches.
</success_criteria>

<output>
After completion, create `.planning/phases/05-watch-rules-notifications/05-02-SUMMARY.md`
</output>
